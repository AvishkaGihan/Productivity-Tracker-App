# ============================================================================
# FASTAPI CONFIGURATION
# ============================================================================

# Application environment: development, staging, production
ENVIRONMENT=development

# API host and port
API_HOST=127.0.0.1
API_PORT=8000

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# SQLite database URL (file-based)
# For development: sqlite:///./db/productivity_tracker.db
# Make sure ./db directory exists or will be created
DATABASE_URL=sqlite:///./db/productivity_tracker.db

# ============================================================================
# AUTHENTICATION & SECURITY
# ============================================================================

# Secret key for JWT token signing
# IMPORTANT: Change this in production! Use a random 32+ character string
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production-use-32-char-minimum

# JWT algorithm for token signing
ALGORITHM=HS256

# Access token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================================================
# GOOGLE GEMINI API CONFIGURATION
# ============================================================================

# Google Gemini API Key
# Get from: https://ai.google.dev/gemini-api/docs/api-key
# Create a new API key in Google Cloud Console
GOOGLE_API_KEY=your-google-gemini-api-key-here

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Enable detailed request/response logging
DEBUG_LOGGING=false

# ============================================================================
# CORS CONFIGURATION
# ============================================================================

# Allowed origins for CORS (comma-separated)
# For development: http://localhost:3000,http://localhost:8081,http://127.0.0.1:3000
# For production: Use specific domain names
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081,http://127.0.0.1:3000,http://127.0.0.1:8000

# ============================================================================
# NOTES FOR PRODUCTION DEPLOYMENT
# ============================================================================

# Before deploying to production:
# 1. Change SECRET_KEY to a strong random value
# 2. Set ENVIRONMENT=production
# 3. Set specific ALLOWED_ORIGINS instead of wildcards
# 4. Use a production database (PostgreSQL recommended)
# 5. Use environment variables for sensitive data (not .env files)
# 6. Enable HTTPS (PROTOCOL=https)
# 7. Set appropriate LOG_LEVEL
# 8. Configure proper error handling and monitoring
# 9. Run with a production ASGI server (Gunicorn with Uvicorn workers)
